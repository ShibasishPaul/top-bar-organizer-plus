Top Bar Organizer Plus v17.1 is a small follow-up to v17.

# Changes

- Preferences widgets no longer look themselves up via `lookupByURL()`; they read settings/metadata/dir through a shared module set once by `prefs.ts`, fixing EGO's shexli EGO-X-006 warning.
